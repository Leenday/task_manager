import { createTheme } from '@material-ui/core/styles';

const MUITheme = createTheme({
  palette: {
    primary: {
      main: '#516dc4',
    },
    secondary: {
      main: '#c75a83',
    },
  },
});

export default MUITheme;
